# Must See Movies

## Setup

 - Clone
 - `cd` into root folder
 - `bundle install`
 - `rake db:migrate`
 - Open the code in Sublime
 - `rails server`
 - Open [http:localhost:3000/movies](http:localhost:3000/movies) in Chrome
